# render-test

> Deterministic parser exercise page.
> Covers headers, descriptions, examples and {{placeholder}} highlighting.

- Run a command with an inline {{argument}} placeholder:

`command --flag {{value}} positional`

- Pipe two commands together:

`first {{input}} | second --out {{output}}`
